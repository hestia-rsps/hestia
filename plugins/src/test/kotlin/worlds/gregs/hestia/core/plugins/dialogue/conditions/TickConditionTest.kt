package worlds.gregs.hestia.core.plugins.dialogue.conditions

import org.junit.jupiter.api.Test

internal class TickConditionTest {

    @Test
    fun `Next returns wait value`() {
        //Given
        /*val value = 4
        coroutine = SuspendingCoroutine(EmptyCoroutineContext, {
            kotlin.suspend(Counter(value))
        }, mock<Counter>())
        coroutine.state = SuspendingCoroutine.State.INITIAL
        whenever(continuation.resume(eq(Unit))).then {
            coroutine.resumeWith(Result.success(Unit))
        }
        //When
        runBlocking {
            coroutine.next()
        }
        //Then
        val condition = coroutine.condition as Counter
        Assertions.assertEquals(value, condition.ticksLeft.get())*/
    }
}